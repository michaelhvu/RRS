#include <iostream.h>
#include <string.h>

using std::string;

class RobotModel {
	private:
		string name;
		int modelNumber;
		double price;
		// Add robot components


	public:
		string getName();
};